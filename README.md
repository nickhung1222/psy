# psy

這是一個 Python 專案，使用 uv 管理虛擬環境。

## 檔案說明
- `chat.json`: 聊天相關的資料檔案。
- `test.ipynb`: Jupyter Notebook 測試檔案。

## 設定
1. 安裝 uv: `pip install uv`
2. 建立虛擬環境: `uv venv`
3. 啟動虛擬環境: `source .venv/bin/activate`
4. 安裝依賴: `uv pip install -r requirements.txt` (如果有的話)

## 使用
請參考 `test.ipynb` 中的範例。
